package group12.model;

/**
 * Enumeration for different types of resource
 * WEB - web resource
 * DEFAULT - resource that user fills with data no external source
 */
public enum ResourceType
{
    WEB,
    DEFAULT
}
